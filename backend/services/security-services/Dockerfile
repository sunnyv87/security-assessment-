FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
RUN pip install --no-cache-dir .

COPY src/ src/

ENV PYTHONPATH=/app
ENV SECURITY_API_PORT=8092

EXPOSE 8092

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8092"]
